{
  "private": true,
  "type": "module",
  "scripts": {
    "init": "resumed init",
    "start": "resumed",
    "inject": "uv run inject-override.py",
    "build": "resumed && uv run inject-override.py"
  },
  "dependencies": {
    "resumed": "^3.0.1",
    "validator": "^13.12.0"
  }
}
